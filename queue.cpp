#include <iostream>
#include <queue>
using namespace std;
int main(){
    queue<int> myqueue;
    queue<int> tempmyqueue;
    myqueue.push(10);
    myqueue.push(420);
    myqueue.push(69);

    if (myqueue.empty())
    {
        cout<<"Skill issue"<<endl;

    }
    else
    {
        cout << "Sucess" << endl;
    }
    cout<<myqueue.size()<<endl;
    while (!myqueue.empty())
    {
        myqueue.()
        cout<<myqueue.front()<<endl;
        myqueue.pop();
    }
    
    
    
}